import logging
import os
logging.basicConfig(format='%(levelname)s: %(asctime)s | Function: %(funcName)s | Line %(lineno)d | Error -> %(message)s', filename='ErrorLog.txt', datefmt='%m/%d/%Y %I:%M:%S %p', level=logging.WARNING)
error = False
def errorFunc():
    try:
        printf(error + 2)
    except Exception as Argument:
        # errorLog = open("ErrorLog.txt", "a")
        # errorLog.write(str(Argument))
        # errorLog.close()
        logging.warning(str(Argument))
        logging.error(str(Argument))
        print(os.stat("ErrorLog.txt").st_size)
    # printf(error + 2)
errorFunc()
